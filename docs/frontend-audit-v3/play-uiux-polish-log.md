# /play UI/UX polish log

- 2026-05-30: Browser QA намери, че mobile action dock-ът е под първия viewport, desktop dock-ът пак прави дълъг документ, а 18-player ring клипва долните seats. Поправка: dock-ът става fixed bottom sheet на mobile, desktop dock-ът се прибира под stage column-а, crowded seats се свиват, а selection auto-разгъва sheet-а.
- 2026-05-30: Добавена директна `PlaySeat` unit защита за target click, accessible selection state, revealed public role и host management actions.
- 2026-05-30 06:01 +03:00: Поправен mismatch между interactive seats и action dock. Dead/spectator voting fixtures вече нямат targetable seats и не получават voting action UI; living voter запазва seat targets и panel. Mobile action dock започва компактен в action phases и се разгъва след избор на seat. Browser: `390x844` dead/spectator/living + `1440x900` living fixtures без console errors/overflow; coordinate click върху mobile seat даде `selectedCount=1` и `data-expanded=true`. Gate: `git diff --check`, focused web tests, `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm build`, `pnpm perf:budget`, `pnpm check:dict` зелени.
- 2026-05-30 06:33 +03:00: Поправен doctor self-protect regress от interactive seats. Stage target list вече не скрива собственото място на Доктора; сървърът остава authoritative за стаи, където self-protect е забранен. Browser: `night/werewolves/doctor/host/8 players` click върху собствен seat даде `aria-pressed=true`, selected target `Искра`, enabled `Пази тази нощ`, без console errors/overflow. Focused `NightActionPanel` тестът е зелен.
- 2026-05-30 06:43 +03:00: Матричен Browser audit: 14 фази × 2 фракции × 2 теми × desktop/mobile без runtime overlay, horizontal overflow, seat overlap или clipped seats. Screenshot QA намери, че cookie banner-ът закрива mobile phase title/stage; play-only CSS го премести като компактна notice лента под navbar-а на mobile и долу вдясно на desktop. Follow-up screenshots: `bannerTitleOverlap=0`, `bannerDockOverlap=0`, `overflowX=false`.
- 2026-05-30 06:52 +03:00: Mobile action dock-ът получи по-плътна материална основа и `Noto Serif Display` heading stack, за да не прозира сцената под collapsed sheet-а и кирилицата да не пада към неподходящ serif fallback. Dev-only Next indicator остава извън app DOM и не е production UI.
- 2026-05-30 07:45 +03:00: Дълбок review на uncommitted M31-M33.5 diff-а. Поправени са role-art family fallback за fixture роли, narrator deck grid crush, 9-12 seat geometry overlap, stale keyboard shortcuts при двуцелеви роли и излишен focus stop в `PlaySeat`. Live QA: desktop/mobile fixture-и без console errors, horizontal overflow, seat/core overlaps или private role text в stage DOM; остава UX препоръка desktop lobby с host/narrator controls да върне по-доминантен stage. Gate: focused tests, `pnpm typecheck && pnpm test && pnpm regression && pnpm playtest`, `pnpm build`, `pnpm perf:budget`, `pnpm check:dict`, `git diff --check`, primitive/motion/reduced-motion guards зелени.
- 2026-05-30 07:51 +03:00: Поправен desktop host/narrator layout budget. `.play-layout:has(.play-narrator-deck)` вече пази stage-а от компресия, cap-ва dock/deck височините и оставя rail-а да скролва вътрешно. Live QA: lobby host light desktop stage `234px -> 378px`, narrator night desktop stage `504px -> 588px`, no-narrator voting unchanged, mobile lobby unchanged; без console errors, horizontal overflow или seat/core overlaps. Gate: `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm perf:budget`, `git diff --check` зелени.
- 2026-05-30 08:15 +03:00: След повторен live audit на текущия worktree се върна host lobby perimeter bug: 12-player werewolves desktop имаше 6 seat overlaps, а 15/18-player crowded host lobby можеше да докосне table core. Корекция: narrator/host dense table inset-ът дава по-висок perimeter (`116-128px` top) и пази долните seats от clipping (`28-36px` bottom). Browser/Playwright temp proof преди commit edit: 12/15/18 host lobby `seatOverlaps=0`, `coreOverlaps=0`, `clipped=0`.
- 2026-05-30 08:22 +03:00: Screenshot QA хвана, че full/crowded desktop H1 още минава под медальони, а stage ledger chip-овете докосват долния десен seat. Корекция: 9+ seats ползват компактна desktop HUD лента (phase pill + H1 на един ред), ledger-ът се качва горе вдясно; timer/table остават центърът.
- 2026-05-30 08:27 +03:00: Повторен Browser/Playwright matrix: 14 фази × 2 фракции × 2 теми × desktop/mobile = 112 клетки, `issueCount=0` за framework overlay, horizontal overflow, seat overlap, table-core overlap, clipped seats и HUD/ledger overlap. Interaction smoke: voting seat click избира `Борил`, `selectedCount=1`, confirm `Потвърди гласа за Борил` enabled; без app console errors (само legacy CSS preload warning).
- 2026-05-30 08:30 +03:00: Gate след stage/HUD корекциите: `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm perf:budget`, `git diff --check`, `pnpm build` зелени. Guard spot-check: primitive override search = 0, Motion imports = 3, без нов CSS `prefers-reduced-motion`.
- 2026-05-30 08:38 +03:00: Desktop host/narrator action dock вече не реже role/action cards като вътрешна таблица. При narrator deck dock-ът се отваря по естествена височина, RoleCard става compact (facts + art + summary, security note скрит само в dock), action panel typography/buttons се свиват и ability icon padding-ът вече не поглъща текста. Screenshot QA: `Провери дали е от Мафията` е четим; matrix 112 клетки остава `issueCount=0`.
- 2026-05-30 08:54 +03:00: Поправен последният dense host lobby overlap при 15/18 играчи. Seat management бутоните вече са absolute hover/focus popover, не grid item с нулева височина, така че скритите `Кмет`/`Разказвач` controls не добавят gap към всеки seat token. Live QA: 9/12/15/18 players × werewolves/mafia × lobby/night/voting `issueCount=0`; wider 56-cell phase/theme/mobile matrix `issueCount=0`; interaction smoke: voting seat избира `Борил`, two-target Blacksmith избира `Борил` + `Рада` и `Изкови меч` е enabled. Gate: `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm build`, `pnpm perf:budget`, `git diff --check`, primitive/motion/reduced-motion guards зелени.
- 2026-05-30 10:24 +03:00: Desktop action dock polish след повторен screenshot QA. RoleCard вече има compact treatment само вътре в `.play-action-dock` (по-ниска карта, кратко summary, 3 compact facts, security note скрит в dock-а), а `HunterRevengePanel` ползва същия `.play-action-buttons` rhythm като voting/night. Проверка: 112 клетки (14 фази × 2 фракции × 2 теми × desktop/mobile) `issueTotal=0` за framework overlay, horizontal overflow, seat/table/HUD/ledger/title overlap и private-role text в stage; interaction smoke: desktop/mobile voting seat selection + two-target Blacksmith confirm зелени. Gate: `git diff --check`, `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm build`, `pnpm perf:budget`, primitive/motion/reduced-motion guards зелени.
- 2026-05-30 11:22 +03:00: След повторен review fix: mobile play cookie notice-ът е вдигнат над stage-а (`top: 54px`), а dense desktop stage title max-width-ът оставя място за ledger chip-овете при дългото `ОТМЪЩЕНИЕ НА ЛОВЕЦА`. Browser plugin runtime fail-на (`browser.nameSession` липсва), затова QA мина през headless Playwright fallback. Проверка: 112 клетки (14 фази × 2 фракции × 2 теми × desktop/mobile) `issueCount=0` за framework overlay, horizontal overflow, seat/table/title/ledger/cookie overlap, clipped seats и private-role text в stage. Gate: `git diff --check`, `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm build`, `pnpm perf:budget` зелени; primitive/Motion/reduced-motion runtime guards без нови нарушения (само docs/README text hits).
- 2026-05-30 11:30 +03:00: Premium readability pass върху dense HUD и cookie notice. Desktop cookie banner-ът вече е slim one-line под navbar-а и приключва преди play rail-а; `ОТМЪЩЕНИЕ НА ЛОВЕЦА` вече се показва цялото вместо ellipsis, без да влиза в ledger-а. Проверка: 112 клетки (14 фази × 2 фракции × 2 теми × desktop/mobile) `issueCount=0`, включително `titleClipped=false`, `titleLedgerOverlap=false`, `cookieStageOverlap=false`, `cookieRailOverlap=false`. Gate: `git diff --check`, `pnpm check:dict`, `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm build`, `pnpm perf:budget` зелени.
- 2026-05-30 12:42 +03:00: Continuation pass върху текущия dirty worktree. Поправен е `doctorCanSelfProtect` exact-optional blocker в visual snapshot shell-а; play atmosphere selector-ите са scope-нати към `.play-shell.game-shell`; 18-player short desktop получи compact crowded table perimeter без clipped seats; short desktop action dock вече показва confirm/skip без вътрешно скриване, включително `hunter_revenge` host/narrator. Browser plugin runtime пак fail-на при `browser.nameSession`, затова QA мина през headless Playwright fallback. Проверка: 168 клетки (14 фази × 2 фракции × 2 теми × desktop/mobile/short-desktop) `issueCount=0` за framework overlay, horizontal overflow, seat/table/title overlap, clipped seats и dock button clipping; interaction smoke: 18-player mafia night seat click избира цел и активира `Пази тази нощ`. Gate: `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm build`, `pnpm perf:budget`, `git diff --check` зелени.
- 2026-05-30 14:51 +03:00: Continuation polish pass. Winner takeover вече скрива личния action dock и narrator deck-а, така че `game_over` не се връща към control-panel chrome под финалния splash. Loading state-ът на stage-а вече рендерира медальонни skeleton seats по периметъра на масата вместо хоризонтални cards вътре в ring-а; desktop shell коментарът е синхронизиран с реалния full-width cockpit dock. Browser runtime пак няма `nameSession`, fallback QA през Playwright: game-over desktop/mobile без action/narrator chrome, no horizontal overflow, no seat clipping/overlap; normal night/voting smoke също чист. Focused `play-room-client` + `PlaySeat` tests и `git diff --check` зелени.
- 2026-05-30 15:05 +03:00: Финалният `game_over` вече превзема целия play shell: chronicle rail-ът не се рендерира, desktop stage-ът span-ва цялата ширина и winner/post-game splash стои като централна сцена, не като panel до side rail. Focused test пази липсата на role/narrator/rail chrome. Playwright matrix: 14 фази × 2 фракции × 2 теми × desktop/mobile = 112 клетки, `issueCount=0` за horizontal overflow, framework overlay, seat/table/title overlap, clipped seats и game-over chrome.
- 2026-05-30 15:18 +03:00: Phase transition overlay-ът получи faction-aware cinematic treatment с наличните transition/phase assets: мафията има лампа/щори/oxblood wash, върколаците луна/мъгла/forest wash, role reveal вече ползва `--art-reveal`. Премахнат е `backdrop-filter` от overlay card-а; новите анимации са само `opacity`/`transform`. Cookie notice-ът вече остава зад cinematic overlay-а (`z-index` 130/135). Screenshot QA чрез Playwright injection: desktop mafia voting + mobile werewolves night без horizontal overflow и без cookie върху overlay-а.
- 2026-05-30 16:13 +03:00: Continuation pass върху desktop app-shell-а. `.play-layout` вече е `"stage rail" / "dock rail"`, така че chronicle rail-ът span-ва stage+dock и dock-ът не влиза под дясната колона. Action dock-ът получи compact card budget и full-width action copy row; `Кого ще изгоните от площада?` и `Докторът пази един играч за тази нощ` вече не се режат. Full-density 9-12 seat inset-ът е върнат нагоре, за да пази stage perimeter след по-висок dock budget. Browser runtime пак fail-на (`browser.nameSession` липсва), QA мина през Playwright fallback: 114 клетки (14 фази × 2 фракции × 2 теми × desktop/mobile + 18-player extras) `issueCount=0` за framework overlay, horizontal overflow, seat/core/title overlaps, clipped seats, dock/rail collision и action-title clipping. Gate: `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm perf:budget`, `pnpm build`, `git diff --check`; primitive/Motion/reduced-motion code guards зелени.
- 2026-05-30 16:26 +03:00: Quiet-phase action dock polish. Когато няма voting/night/hunter/lobby action, desktop dock-ът вече не стои като празна parchment площ: получи role-only “dossier alcove” с phase emblem, etched texture и compact RoleCard treatment без недовършени fact chips. Light theme остава парчмент, dark theme остава noir room. Screenshot QA: `day_discussion/mafia/light` вече изглежда като нарочен таен ъгъл, не празен панел. Matrix QA: 114 клетки `issueCount=0` за framework overlay, horizontal overflow, seat/core/title overlaps, clipped seats, dock/rail collision, action-title clipping и role-only dock overflow над tolerance.
- 2026-05-30 18:41 +03:00: Continuation dossier pass. Quiet/lobby desktop action dock вече не реже private result-а в RoleCard: result paragraph получи собствен `.role-card-result` treatment, quiet dock-ът има малко повече височина, а lobby dock-ът става двуколонен (ready/start controls + compact role dossier). Browser runtime се закачи за navigation/log checks, но screenshot capture timeout-на, затова visual evidence мина през Playwright fallback. QA: 112 клетки (14 фази × 2 фракции × 2 теми × desktop/mobile) `issueCount=0`; interaction smoke: voting seat избира `Борил` и активира `Потвърди гласа за Борил`, Blacksmith избира две цели и активира `Изкови меч`.
- 2026-05-30 19:12 +03:00: Дълбок review на текущите uncommitted Claude промени. Поправени са два UI риска: mobile action dock-ът вече е под modal/toast слоевете (`z-index: 70`), а host/narrator seat controls получиха inward placement (`data-menu-x/y`), така че popover-ите не се клипват по stage краищата. Browser QA: mobile `390x844` lobby/voting без overflow, shortcut modal `80 > dock 70`, reconnect modal `86 > dock 70`, всички 4 menu placement посоки `clipped=false` desktop/mobile; desktop night shell без console errors. Gate: `git diff --check`, `pnpm typecheck`, `pnpm test`, `pnpm regression`, `pnpm playtest`, `pnpm check:dict`, `pnpm build`, `pnpm perf:budget` зелени.
- 2026-05-30 19:26 +03:00: Premium table-core pass. Центърът на масата вече има faction-specific материал: върколаци = лунен камък/кладенец с фини концентрични следи и пукнатини; мафия = зелен felt/card-table weave с месингов inlay. Първият conic вариант за върколаци беше твърде “спици”, затова е омекотен към `repeating-radial-gradient`. Playwright screenshot QA: desktop werewolves night + mobile werewolves voting + mafia day/night, timer-ът остава видим и вътре в core-а. Matrix: 112 клетки `issueCount=0` за overflow, seat/core/title/ledger overlaps, clipped seats, game-over chrome и timer clipping. Gate: `git diff --check`, `pnpm regression`, `pnpm typecheck`, `pnpm test`, `pnpm playtest`, `pnpm check:dict`, `pnpm build`, `pnpm perf:budget` зелени.
- 2026-05-30 20:18 +03:00: Continuation fix след deep review. 18-player crowded desktop вече разпределя медальоните по 5 top/bottom + повече side slots и свива dense tokens; доказателство: `players=18` върколаци light desktop `seatOverlaps=0`, `overflowX=false`. `Escape` вече чисти избрана seat цел дори когато focus-ът е върху action button, но остава ненатрапчив в text-entry controls. Browser plugin се закачи за page identity/console checks; interaction proof и 115-cell matrix минаха през Playwright fallback: `issueCount=0` за overlay, overflow, seat/core/title/ledger overlaps, clipped seats, dock button clipping и game-over chrome.
- 2026-05-30 20:31 +03:00: Layout hardening pass. Play desktop shell вече не зависи от `.play-layout:has(...)` за narrator/takeover/action режими; `play-room-client` подава явни `data-has-narrator-deck`, `data-dock-has-ritual` и `data-stage-takeover` flags, а `PlayRoom.module.css` чете тях. Това пази cinematic composition-а стабилен при бъдещи DOM промени. Browser smoke: lobby host има `data-has-narrator-deck=true`, без console errors/overflow. Matrix: 115 клетки `issueCount=0`.
- 2026-05-30 22:02 +03:00: Continuation QA fix. `VoteTallyBar` вече е focusable `region`, когато докът го прави scrollable, така че axe `scrollable-region-focusable` не се връща. Play-only cookie notice-ът е свален до `66px` и на desktop, за да няма subpixel stage overlap. Focused `VoteTallyBar` test зелен.
- 2026-05-30 22:26 +03:00: A11y/layout hardening. Cookie notice-ът вече не е `aside role=dialog`, а play rail-ът е вътрешен labeled `section`, така че manual axe sweep на `/play` е без structural violations. Desktop lobby dock-ът разшири control колоната и стегна ready/start pill-овете; 112-cell matrix + 18-player density smoke са `issueCount=0`, interaction smoke за seat click/keyboard Escape е зелен.
